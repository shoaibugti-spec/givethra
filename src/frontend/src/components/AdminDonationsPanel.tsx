import { useEffect, useState } from "react";
import { Check, RefreshCw, X } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { adminGetContributionSpending, adminGetDonations, adminSpendContribution, adminUpdateDonation } from "@/lib/api";

const money = (value: number) => `PKR ${Number(value || 0).toLocaleString()}`;
export default function AdminDonationsPanel() {
  const [donations, setDonations] = useState<any[]>([]);
  const [spending, setSpending] = useState<any[]>([]);
  const [caseId, setCaseId] = useState("");
  const [amount, setAmount] = useState("");
  const [busy, setBusy] = useState(false);
  const load = async () => { try { const [d, s] = await Promise.all([adminGetDonations(), adminGetContributionSpending()]); setDonations(Array.isArray(d) ? d : []); setSpending(Array.isArray(s) ? s : []); } catch (e: any) { toast.error(e?.message || "Donation data could not be loaded"); } };
  useEffect(() => { void load(); }, []);
  const update = async (id: string, status: string) => { setBusy(true); try { await adminUpdateDonation(id, { status }); toast.success(`Donation ${status}`); await load(); } catch (e: any) { toast.error(e?.message || "Update failed"); } finally { setBusy(false); } };
  const spend = async () => { const value = Number(amount); if (!caseId.trim() || !Number.isFinite(value) || value <= 0) return toast.error("Enter an active case ID and valid amount"); setBusy(true); try { await adminSpendContribution({ case_id: caseId.trim(), amount: value }); toast.success("Contribution allocated to case"); setCaseId(""); setAmount(""); await load(); } catch (e: any) { toast.error(e?.message || "Contribution allocation failed"); } finally { setBusy(false); } };
  const pending = donations.filter((item) => item.status === "pending");
  return <div className="space-y-4"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Community Donations</h2><p className="text-sm text-muted-foreground">Separate from the Credits Wallet. Approve verified contributions before they enter the community ledger.</p></div><Button variant="outline" size="sm" onClick={() => void load()}><RefreshCw className="mr-2 h-4 w-4" />Refresh</Button></div><div className="grid gap-4 md:grid-cols-3"><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold">{pending.length}</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Approved / completed</p><p className="text-2xl font-bold">{donations.filter((item) => ["approved", "completed"].includes(item.status)).length}</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Spending records</p><p className="text-2xl font-bold">{spending.length}</p></CardContent></Card></div><Card><CardHeader><CardTitle>Donation review</CardTitle></CardHeader><CardContent className="space-y-3">{donations.length === 0 ? <p className="text-sm text-muted-foreground">No donation records.</p> : donations.map((item) => <div key={item.id} className="grid gap-3 rounded-xl border p-4 md:grid-cols-[1fr_auto] md:items-center"><div><div className="flex flex-wrap items-center gap-2"><span className="font-semibold">{money(item.amount)}</span><span className="rounded-full bg-muted px-2 py-0.5 text-xs capitalize">{item.status}</span><span className="text-xs text-muted-foreground">{item.frequency}</span></div><p className="mt-1 text-sm">{item.donor_name || item.donor_email || item.user_id}</p><p className="text-xs text-muted-foreground">{item.payment_method} · {item.payment_reference} · {new Date(item.submitted_at).toLocaleString()}</p>{item.proof_url && <a className="text-xs text-primary underline" href={item.proof_url} target="_blank" rel="noreferrer">View proof</a>}</div>{item.status === "pending" && <div className="flex gap-2"><Button size="sm" disabled={busy} onClick={() => void update(item.id, "approved")}><Check className="mr-1 h-4 w-4" />Approve</Button><Button size="sm" variant="outline" disabled={busy} onClick={() => void update(item.id, "rejected")}><X className="mr-1 h-4 w-4" />Reject</Button></div>}</div>)}</CardContent></Card><Card><CardHeader><CardTitle>Contribution Spending</CardTitle></CardHeader><CardContent className="space-y-3"><div className="grid gap-2 md:grid-cols-[1fr_180px_auto]"><Input placeholder="Approved active case ID" value={caseId} onChange={(e) => setCaseId(e.target.value)} /><Input placeholder="Amount (PKR)" inputMode="numeric" value={amount} onChange={(e) => setAmount(e.target.value.replace(/[^0-9.]/g, ""))} /><Button disabled={busy} onClick={() => void spend()}>Allocate</Button></div>{spending.length === 0 ? <p className="text-sm text-muted-foreground">No spending records yet.</p> : spending.map((item) => <div key={item.id} className="flex justify-between rounded-lg border p-3 text-sm"><span>{item.case_title || item.case_id}</span><span className="font-semibold">{money(item.amount)} · {item.status}</span></div>)}</CardContent></Card></div>;
}
